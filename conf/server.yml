# IP du serveur HTTP
ip: "127.0.0.1"

# Port
port: 80

# Backlog (File d'attente)
backlog: 10

# Timeout des requêtes
timeout: 30

# Dossier où se trouvent les fichiers du serveur web depuis server.c
www: "./www"
